all:
  children:

    swarm_manager:
      hosts:
        pterodactyl.diesel.net:

  vars:

    host: pterodactyl.diesel.net
    
    ansible_user: automation
    ansible_python_interpreter: /usr/bin/python3